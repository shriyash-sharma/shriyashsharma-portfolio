node_modules/
.DS_Store
*.log
.env
.env.*
!.env.example

# Local Docker overrides (may contain DATABASE_URL and API keys)
docker-compose.override.yml

# Next.js
.next/
out/

# Python
.venv/
__pycache__/
*.py[cod]
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Build/cache
.turbo/

# Database backups (local pg_dump output — never commit)
backups/

# Editor local history (keep on disk, never commit)
.history/
