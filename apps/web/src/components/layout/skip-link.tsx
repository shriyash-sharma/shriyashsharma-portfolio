/**
 * Keyboard-only bypass link.
 * Keeps the premium layout quiet visually while satisfying skip navigation.
 */
export function SkipLink() {
  return (
    <a href="#main-content" className="skip-link">
      Skip to content
    </a>
  );
}
